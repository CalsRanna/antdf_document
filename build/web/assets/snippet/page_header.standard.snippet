import 'package:ant_design_flutter/antdf.dart';

void main() {
  runApp(const AntApp(home: PageHeaderStandardDemo()));
}

class PageHeaderStandardDemo extends StatelessWidget {
  const PageHeaderStandardDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AntScaffold(
      body: PageHeader(
        subtitle: Text('This is a subtitle'),
        title: Text('Title'),
      ),
    );
  }
}
